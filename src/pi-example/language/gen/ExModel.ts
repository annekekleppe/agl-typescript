// Generated by the ProjectIt Language Generator.
import { observable } from "mobx";
import { model, MobxModelElementImpl, observablelistpart, PiNamedElement, PiUtils } from "@projectit/core";
import { Entity, Method, ExampleMetaType } from "./internal";

/**
 * Class ExModel is the implementation of the model unit with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to changes in the state of its properties.
 */
@model
export class ExModel extends MobxModelElementImpl implements PiNamedElement {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data
     */
    static create(data: Partial<ExModel>): ExModel {
        const result = new ExModel();
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.entities) {
            data.entities.forEach(x => result.entities.push(x));
        }
        if (!!data.methods) {
            data.methods.forEach(x => result.methods.push(x));
        }
        return result;
    }

    readonly $typename: ExampleMetaType = "ExModel"; // holds the metatype in the form of a string
    $id: string; // a unique identifier
    @observable name: string = ""; // implementation of name
    entities: Entity[]; // implementation of part 'entities'
    methods: Method[]; // implementation of part 'methods'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = PiUtils.ID(); // uuid.v4();
        }
        // both 'observablepart' and 'observablelistpart' change the get and set of an attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable
        observablelistpart(this, "entities");
        observablelistpart(this, "methods");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    piLanguageConcept(): ExampleMetaType {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    piId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    piIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    piIsUnit(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    piIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    piIsBinaryExpression(): boolean {
        return false;
    }
}
